#include <bits/stdc++.h>
using namespace std;

#define MAXN 100010
typedef long long ll;

int T,c,n;
ll L,q,v[MAXN];

int main(){
	freopen("input","r",stdin);
	cin>>T>>c;
	while(T--){
		cin>>n>>L>>q;
		for(int i=1;i<=n;i++)scanf("%lld",&v[i]);
		sort(v+1,v+n+1);
		for(int i1)
	}
	return 0;
}
